id: test-coverage
name: "Test Coverage Threshold"
description: >
  Test coverage must be >= 95%. Medical/defense grade.
category: common
severity: major
weight: 5

ast_checks:
  - type: file_structure
    check: "test_dir_exists"
    description: "Test directory must exist"

scoring:
  method: boolean
  pass_threshold: 100
